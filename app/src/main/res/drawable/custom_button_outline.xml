<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Outline stroke -->
    <stroke
        android:width="2dp"
        android:color="#FF0174BE" />

    <!-- Transparent background -->
    <solid android:color="@android:color/transparent" />

    <!-- Corner radius -->
    <corners android:radius="44dp" />
</shape>
